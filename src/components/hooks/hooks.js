import React, { useState, useEffect } from 'react'

const hook = () => {
    // const [ text, setText] = useState('Waiting for Render')
//   useEffect(() => setText({ text: 'Hook on Render!' }));
  return (
    <React.Fragment>
      {/* <h3>{text}</h3> */}
    </React.Fragment>
  )
}

export default hook;